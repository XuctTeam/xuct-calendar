<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="cn.com.xuct.calendar.ums.boot.mapper.UserRoleMapper">
    <resultMap type="cn.com.xuct.calendar.common.module.dto.UserInfoDto" id="userResultMap">
        <!--team中的基本属性-->
        <id column="userId" property="userId"/>
        <result column="username" property="username"/>
        <result column="password" property="password"/>
        <result column="status" property="status"/>
        <!--关联属性的映射关系-->
        <collection property="roles" ofType="string" javaType="list">
            <result column="roleCode"/>
        </collection>
    </resultMap>

    <select id="selectUserByUserName" parameterType="string" resultMap="userResultMap">
        select r.code roleCode, u.id userId, u.user_name username, u.password password, u.status status
        from sys_user_role ur
                 left join sys_role r on ur.role_id = r.id
                 left join sys_user u on u.id = ur.user_id
        where u.user_name = #{userName}
    </select>

</mapper>